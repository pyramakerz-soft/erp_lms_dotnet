import { DetailedPermission } from './detailed-permission';

describe('DetailedPermission', () => {
  it('should create an instance', () => {
    expect(new DetailedPermission()).toBeTruthy();
  });
});
