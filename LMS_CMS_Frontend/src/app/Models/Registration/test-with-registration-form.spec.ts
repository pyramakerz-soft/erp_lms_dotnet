import { TestWithRegistrationForm } from './test-with-registration-form';

describe('TestWithRegistrationForm', () => {
  it('should create an instance', () => {
    expect(new TestWithRegistrationForm()).toBeTruthy();
  });
});
