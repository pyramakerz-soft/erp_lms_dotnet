import { RegisterationFormTestAnswer } from './registeration-form-test-answer';

describe('RegisterationFormTestAnswer', () => {
  it('should create an instance', () => {
    expect(new RegisterationFormTestAnswer()).toBeTruthy();
  });
});
