import { StudentPerformance } from './student-performance';

describe('StudentPerformance', () => {
  it('should create an instance', () => {
    expect(new StudentPerformance()).toBeTruthy();
  });
});
