import { RegisterationFormTest } from './registeration-form-test';

describe('RegisterationFormTest', () => {
  it('should create an instance', () => {
    expect(new RegisterationFormTest()).toBeTruthy();
  });
});
