import { EmployeePermission } from './employee-permission';

describe('EmployeePermission', () => {
  it('should create an instance', () => {
    expect(new EmployeePermission()).toBeTruthy();
  });
});
