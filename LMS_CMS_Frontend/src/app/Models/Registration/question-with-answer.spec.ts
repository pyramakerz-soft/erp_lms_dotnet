import { QuestionWithAnswer } from './question-with-answer';

describe('QuestionWithAnswer', () => {
  it('should create an instance', () => {
    expect(new QuestionWithAnswer()).toBeTruthy();
  });
});
